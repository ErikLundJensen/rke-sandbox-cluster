#!/bin/bash


# Generate user/password for Kibana UI

mkdir -p tmp

echo -n `pwgen -s 16 -1` > ./tmp/kibana-password.plain
htpasswd -cb auth kibana `cat ./tmp/kibana-password.plain`
kubectl create secret generic kibana-basic-auth --from-file=auth -n ingress-nginx
